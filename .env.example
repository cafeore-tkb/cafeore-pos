# サーバーが起動している機器のIPアドレス
# デフォルトは localhost ですが、外部マシンから接続する場合は
# このIPアドレスをホストマシンのIPアドレス (例: 192.168.1.100) に変更してください
HOST_IP=localhost

# APIサーバーのベースURL
# 上記のHOST_IPが自動的に反映されます
VITE_API_BASE_URL=http://${HOST_IP}:8080
